#include "main.h"

/**
 * _strlen - return the length of a string
 * written by Rukevwe
 * @s: char to check
 * Return 0
 */

int _strlen(char *s)
{
	int a = o;

	for (; *s++;)
		a++;
	return (a);
}

